import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:salu_licence_manager/app/app_module.dart';

void main() {
  initModule(AppModule());
  // UserController user;
  //
  setUp(() {
    //     user = AppModule.to.get<UserController>();
  });

  group('UserController Test', () {
    //   test("First Test", () {
    //     expect(user, isInstanceOf<UserController>());
    //   });

    //   test("Set Value", () {
    //     expect(user.value, equals(0));
    //     user.increment();
    //     expect(user.value, equals(1));
    //   });
  });
}
